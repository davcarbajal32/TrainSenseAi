# Copy to .env and fill in real values. NEVER commit .env to git.
# On EC2: create this file in ~/TrainSenseAi/.env

SECRET_KEY=

# MongoDB Atlas - use the rotated password, not the one you exposed earlier
MONGO_URI=
MONGO_DB_NAME=trainsense

# For session 3
ANTHROPIC_API_KEY=
